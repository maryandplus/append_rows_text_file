# append_rows_text_file
Append text files by row 

Files must have same ammount of rows. The third element of each row from each file is added to a new text file. Different lines are split with break lines.
